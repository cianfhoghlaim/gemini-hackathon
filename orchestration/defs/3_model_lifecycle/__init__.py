"""orchestration.defs.3_model_lifecycle — the Layer 3 Dagster asset group.

The 5-layer ``defs/`` tree convention (per the canonical cianfhoghlaim
orchestration platform) gives every layer its own sub-package. Layer 3
holds **model-lifecycle** assets — the training/extraction/embedding
steps that produce model + structural artifacts.
"""
