# Ignore the raw PDFs (they are large + reproducible from the upstream source)
# but keep the INDEX.yaml manifest + the placeholder JSON for the deferred
# download so the canonical substrate is reproducible.
*.pdf
!*.placeholder.json
