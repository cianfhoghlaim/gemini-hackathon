"""orchestration.defs.3_model_lifecycle.sensors — the sensor sub-package.

The canonical sub-package for the Layer 3 sensors (per the 5-layer
``defs/`` tree convention). Currently hosts:

  - uk_ncce_pdf_sensor.py : the UK NCCE learning-graph sensor that
    fires when a new NCCE PDF lands in
    ``data/bi_ep/syllabi_raw/uk_ncce/curriculum/``
"""
