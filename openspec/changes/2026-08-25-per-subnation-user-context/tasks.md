# Tasks for per-subnation-user-context

- [x] **T1**: gemini_hackathon/session/schema.py with 8 jurisdictions + 10
  boards + 31 subjects. Public roster excludes the future expansion
  pack subnations from the primary surface.
- [x] **T2**: web SessionContext + localStorage-backed session helpers.
  Onboarding picker is 3-step (subnation → role → cycle).
- [x] **T3**: ModelPolicyBadge component on the home page.
- [x] **T4**: DuckDB-WASM leaderboard + document explorer reading the
  same `.duckdb` file the Python comparison harness writes.
- [x] **T5**: public_roster snapshot in compare summary so docs / UI
  verify the hackathon-profile models were eligible to be compared.
