# Mirror of pyproject.toml dependencies for pip users.
# Canonical dependency source is pyproject.toml — edit that file first.
# Run `uv sync` (preferred) or `pip install -r requirements.txt` (fallback).
#
# Versions pinned to the repos verified against:
# - google-adk 2.7.x (support-memory-lab)
# - gradio 5.28+ (gr.Workflow + gr.mcp + ZeroGPU)
# - huggingface_hub 0.30+ (gradio_client, hf CLI, hf skills add)

# ADK 2 agent runtime — the canonical agent framework
google-adk>=2.7.1,<3.0

# Gradio 5.28+ for gr.Workflow (editorial canvas), gr.mcp (Spaces-as-MCP),
# ZeroGPU (@spaces.GPU), server-side rendering
gradio>=5.28.0,<6.0

# HuggingFace Hub — for gradio_client + the `hf` CLI + hf skills add
huggingface_hub>=0.30

# FastMCP for the per-subject MCP servers
fastmcp>=0.4

# Data ingestion (DLT + DuckDB — the offline dev default)
dlt[bigquery]>=1.0,<2.0
duckdb>=1.0
ibis-framework[duckdb]>=9.0

# LLM routing + extraction (BAML + LiteLLM)
baml-py>=0.223.0
litellm>=1.50

# GCP-first data + AI substrate (Phases 1-6 of the GCP-first refactor)
google-cloud-storage>=2.18
google-cloud-firestore>=2.19
google-cloud-aiplatform>=1.70
google-cloud-documentai>=2.30
google-cloud-workflows>=1.16
google-cloud-scheduler>=2.13
google-cloud-pubsub>=2.23
google-cloud-trace>=1.13
google-cloud-logging>=3.11

# Vector + graph — Firestore FindNearest is the default VectorTarget
networkx>=3.0
numpy>=1.26

# Observability (structlog always-on; Langfuse/MLflow are optional
# cianfhoghlaim-parity exporters — see pyproject.toml's
# `cianfhoghlaim-parity` dependency group)
structlog>=24.0

# Agent memory (Letta)
letta>=0.50

# Web scraping + PDF
firecrawl-py>=1.0
pypdf>=4.0
pyjwt>=2.8
pypdfium2>=4.0
pillow>=10.0

# Reactive notebooks
marimo>=0.10

# Schemas + settings + HTTP
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
httpx>=0.27
requests>=2.31

# --- dev (not auto-installed by `pip install -r requirements.txt`) ---
# Install with: pip install -r requirements.txt -r requirements-dev.txt
